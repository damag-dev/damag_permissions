CREATE TABLE IF NOT EXISTS `szafka` (
  `identifier` varchar(50) DEFAULT NULL,
  `job` varchar(50) DEFAULT NULL,
  `szafka` varchar(50) NOT NULL DEFAULT 'Brak',
  `firstname` varchar(50) DEFAULT NULL,
  `lastname` varchar(50) DEFAULT NULL
);
